# Factorial Calculator

A simple Python application that calculates the **factorial of a number** entered by the user.

## Features

* Calculates the factorial of a positive integer.
* Takes input from the user through the terminal.
* Validates negative numbers.
* Handles invalid or non-numeric input.
* Simple and beginner-friendly program.

## Requirements

* Python 3.x
* NumPy

## Installation

1. Clone the repository.
2. Open the project folder in the terminal.
3. Install the required dependencies:

```bash
pip install -r requirements.txt
```

## Usage

Run the program using:

```bash
python factorial.py
```

Enter a number when prompted:

```text
Enter a number: 5
```

### Output

```text
120
```

## How It Works

The program:

1. Takes a number as input from the user.
2. Checks whether the input is valid.
3. Checks for negative numbers.
4. Uses a loop to multiply all numbers from `1` to the given number.
5. Displays the calculated factorial.

For example:

**5! = 5 × 4 × 3 × 2 × 1 = 120**

## License

This project is licensed under the **MIT License**.
